// Local dev fallback — copy to config.js and add your key.
// config.js is gitignored; never commit your real key.
// On the live server the key comes from .env via api.php?action=config instead.
window.GEMINI_API_KEY = 'your_gemini_api_key_here';
